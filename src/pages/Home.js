import React from "react";
import ImageSlider from "../components/ImageSlider"; // Đảm bảo đường dẫn đúng

const Home = () => {
  return (
    <div className="home">
      <ImageSlider /> {/* Add the ImageSlider component */}
    </div>
  );
};

export default Home;
